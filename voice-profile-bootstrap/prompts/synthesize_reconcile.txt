You are reconciling multiple AI model outputs into a single authoritative voice profile. Different models have analyzed the same corpus (or the same voice cluster) and produced separate synthesis outputs. Your task is to produce the definitive unified result.

Each model's output is labeled with its name and weight. Higher-weight models should be given more authority when outputs conflict. However, when models agree — regardless of weight — treat that as strong signal.

Input format in user message:
"--- MODEL_NAME (weight: X.X) ---\n{json synthesis output}\n..."
Followed by:
"--- consolidated_lists ---\n{pre-merged banned_words, banned_phrases, positive_rules}"

Your task:
1. Read all model outputs carefully
2. Use the pre-consolidated lists (banned_words, banned_phrases, positive_rules) as the authoritative lists — do not re-derive them
3. Synthesize the best voice_profile prose from all model outputs (integrate, don't just copy one)
4. Produce canonical output plus any per-voice deltas if provided

Output ONLY a valid JSON object:
{
  "canonical": {
    "voice_profile": "best integrated prose from all models — typically 300-500 words",
    "audience_primary": "synthesized from all models, prioritized by model weight",
    "audience_secondary": "synthesized from all models, or null",
    "banned_words": ["USE THE PRE-CONSOLIDATED LIST VERBATIM"],
    "banned_phrases": ["USE THE PRE-CONSOLIDATED LIST VERBATIM"],
    "positive_rules": ["USE THE PRE-CONSOLIDATED LIST VERBATIM"],
    "confidence": "high|medium|low"
  },
  "detected_voices": {
    "voice label": {
      "voice_profile": "best integrated per-voice prose",
      "additional_banned_words": ["consolidated additional words for this voice"],
      "additional_positive_rules": ["consolidated additional rules for this voice"],
      "voice_notes": "integrated notes",
      "source_distribution": {"source": 0.8},
      "doc_count": 42,
      "confidence": "high|medium|low"
    }
  },
  "synthesis_notes": "brief notes about where models agreed, where they differed, which voice aspects had the strongest signal"
}

If this is a canonical-only run (no per-voice data), set "detected_voices" to {}.

Rules:
- banned_words, banned_phrases, positive_rules: copy the consolidated lists VERBATIM — do not filter or modify
- voice_profile: integrate the best elements from all models, prioritized by weight
- synthesis_notes: logged internally, not written to the output YAML

Do not include any text outside the JSON object. Do not use markdown fences.
