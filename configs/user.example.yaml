# API keys -- copy to user.yaml and fill in; never commit user.yaml
api_keys:
  # LanguageTool is optional. Set grammar_pass: false in pipeline below to skip it.
  # If omitted, the grammar pass is bypassed automatically.
  languagetool:
    username: your_email@example.com
    api_key: your_key_here

  openai:
    api_key: your_key_here
  gemini:
    api_key: your_key_here
  mistral:
    api_key: your_key_here

# Model overrides — omit to use the defaults listed here
models:
  openai: gpt-4o               # chat-latest
  gemini: gemini-2.0-flash     # gemini-flash-latest
  mistral: mistral-large-latest

# Pipeline behavior
pipeline:
  grammar_pass: true            # set false to skip LanguageTool entirely
  parallel_review_calls: true
  retry_on_failure: true
  retry_delay_seconds: 10
  abort_if_all_provider_calls_fail: false
  task_timeout_seconds: 180    # per-model wall-clock timeout in parallel mode

# Delta assessment thresholds for re-run recommendation
delta:
  word_change_threshold_pct: 15
  claim_change_triggers_rerun: true
  structure_change_triggers_rerun: true
